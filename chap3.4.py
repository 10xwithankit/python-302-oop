# Define a function `greet` that prints a greeting message

# Assign the function `greet` to a new variable `f`
# `f` now points to the same function as `greet`

# Call the function using the new variable `f`
