# Define a variable `message` and assign it a string value
message = "hello world"

# Convert all characters in `message` to uppercase
print(message.upper())  # Output: HELLO WORLD

# Replace a specific substring in `message` with a new value
print(message.replace("hello", "hi"))  # Output: hi world
